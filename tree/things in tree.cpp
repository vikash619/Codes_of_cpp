#include <iostream>
using namespace std;

struct node
{
  int data;
  struct node *left;
  struct node *right;
};

int main()
{

  // Depth = No. of edges from root to node
  // Height = No. of edges from node to depest leaf
  // Siblings = Nodes belong to same parent
  // Leaf = Node that have no child
  // Binary Tree = Tree with max two children for each node or no node
  return 0;
}
